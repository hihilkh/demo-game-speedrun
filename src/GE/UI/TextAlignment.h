#pragma once

#include <stdint.h>

namespace GE::UI
{
	enum class TextVerticalAlignment : uint32_t
	{
		Top,
		Middle,
		Bottom,
	};

	enum class TextHorizontalAlignment : uint32_t
	{
		Left,
		Center,
		Right,
	};
}
