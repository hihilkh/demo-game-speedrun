#pragma once

#include <cstdint>

namespace GE::UI
{
	enum class TextVerticalAlignment : std::uint8_t
	{
		Top,
		Middle,
		Bottom,
	};

	enum class TextHorizontalAlignment : std::uint8_t
	{
		Left,
		Center,
		Right,
	};
}
